<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BinaAhli" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/primary_light</item>
        <!-- Secondary brand color. -->
        <item name="fontFamily">@font/nunito_regular</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/secondary_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTextView.Regular" parent="Widget.AppCompat.TextView">
        <item name="color">@color/black</item>
        <item name="fontFamily">@font/nunito_regular</item>
    </style>

    <style name="AppTextView.Light" parent="Widget.AppCompat.TextView">
        <item name="color">@color/black</item>
        <item name="fontFamily">@font/nunito_light</item>
    </style>

    <style name="AppTextView.Medium" parent="Widget.AppCompat.TextView">
        <item name="color">@color/black</item>
        <item name="fontFamily">@font/nunito_medium</item>
    </style>

    <style name="AppTextView.Bold" parent="Widget.AppCompat.TextView">
        <item name="color">@color/black</item>
        <item name="fontFamily">@font/nunito_bold</item>
    </style>

    <style name="Theme.BinaAhli.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="GreenBtn">
        <item name="android:background">@drawable/bg_button_fill_round_green</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="YellowBtn">
        <item name="android:background">@drawable/bg_button_fill_round_yellow</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.BinaAhli.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BinaAhli.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>